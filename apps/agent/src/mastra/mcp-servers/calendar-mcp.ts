import { MCPServer } from '@mastra/mcp';

export const calendarMCPServer = new MCPServer({
  name: 'calendar',
  description: 'Calendar operations and management server',
  version: '1.0.0',
  tools: [],
  resources: [],
});