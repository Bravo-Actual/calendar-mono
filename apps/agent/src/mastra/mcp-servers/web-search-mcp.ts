import { MCPServer } from '@mastra/mcp';

export const webSearchMCPServer = new MCPServer({
  name: 'web-search',
  description: 'Web search server for calendar context',
  version: '1.0.0',
  tools: [],
  resources: [],
});